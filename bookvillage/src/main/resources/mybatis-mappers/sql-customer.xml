<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mi.bookvillage.customer.model.dao.CustomerDAO">

    <select id="getListCustomer" resultType="CustomerVO">
        SELECT USER_SEQ
             , USER_NAME
             , USER_ID
             , USER_PW
             , DATE_FORMAT(USER_BIRTH, '%Y-%m-%d') AS USER_BIRTH
             , USER_TELL
             , DATE_FORMAT(USER_REG_DT, '%Y-%m-%d %H:%i') AS USER_REG_DT
             , DATE_FORMAT(USER_UPDATE_DT, '%Y-%m-%d %H:%i') AS USER_UPDATE_DT
             , USER_ROLE
          FROM CUSTOMER
    </select>








    
</mapper>